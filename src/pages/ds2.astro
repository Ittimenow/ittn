---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import UiIcon from '../components/UiIcon.astro';

const services = (await getCollection('services')).sort((a, b) => a.data.order - b.data.order);
const cases = (await getCollection('cases')).sort((a, b) => a.data.order - b.data.order).slice(0, 3);

const navigation = [
	['Токены', '#tokens'],
	['Текст', '#typography'],
	['Действия', '#actions'],
	['Формы', '#forms'],
	['Секции', '#sections'],
	['Продажи', '#sales'],
	['Bitrix24', '#bitrix24'],
	['Интерфейс', '#interface'],
];

const palette = [
	{ name: 'Primary', value: '#232BFF', usage: 'CTA, ссылки, активные состояния' },
	{ name: 'Secondary', value: '#5F66FF', usage: 'поддерживающие акценты' },
	{ name: 'Tertiary', value: '#8B5CFF', usage: 'графики и мягкие выделения' },
	{ name: 'Ink', value: '#18181B', usage: 'заголовки, темные поверхности' },
	{ name: 'Neutral', value: '#71717A', usage: 'описания, метаданные' },
	{ name: 'Border', value: '#E4E4E7', usage: 'линии, разделители' },
	{ name: 'Success', value: '#0F9F6E', usage: 'рост, готовность, выгода' },
	{ name: 'Warning', value: '#D97706', usage: 'лимиты, ожидание, внимание' },
];

const typography = [
	{
		name: 'display-lg',
		size: '72px',
		weight: '500',
		leading: '72px',
		text: 'Цифровые системы эффективного бизнеса',
		note: 'System Font / первый экран и большие офферы',
	},
	{
		name: 'body-md',
		size: '16px',
		weight: '400',
		leading: '26px',
		text: 'Сначала фиксируем бизнес-задачу и границы этапа, потом настраиваем инструменты и обучаем команду.',
		note: 'System Font / основной текст',
	},
	{
		name: 'label-md',
		size: '14px',
		weight: '500',
		leading: '20px',
		text: 'Получить консультацию / CRM / SLA 24/7',
		note: 'System Font / кнопки, бейджи, навигация, подписи',
	},
];

const spacing = ['4', '8', '12', '16', '20', '24', '32', '40', '48', '64', '96', '120'];
const radii = ['2', '16', '24', '40', '64', '9999'];

const actionButtons = [
	{ label: 'Получить консультацию', variant: 'primary', icon: 'arrow' },
	{ label: 'Смотреть кейсы', variant: 'secondary', icon: 'briefcase' },
	{ label: 'Обсудить проект', variant: 'dark', icon: 'message' },
	{ label: 'Подробнее', variant: 'ghost', icon: 'chevron' },
];

const chips = ['CRM', 'ИИ-ассистент', 'B2B-портал', 'SLA 24/7', '1С', 'RAG', 'BPMN', 'Аудит'];

const selectOptions = [
	'Внедрение Bitrix24',
	'Сайты и сервисы',
	'ИИ-ассистент',
	'Сопровождение IT-проектов',
	'Интеграции с 1С',
];

const callouts = [
	{
		type: 'info',
		title: 'Коммерческий акцент',
		text: 'Показывайте следующий шаг рядом с выгодой: расчет, аудит, демонстрация, созвон.',
	},
	{
		type: 'success',
		title: 'Готово к запуску',
		text: 'Сценарий согласован, роли настроены, команда прошла обучение.',
	},
	{
		type: 'warning',
		title: 'Нужна оценка',
		text: 'Сложные интеграции и миграции лучше выносить в отдельный этап аудита.',
	},
];

const stats = [
	['150+', 'реализованных проектов'],
	['2013', 'год начала работы'],
	['99.97%', 'uptime за первый год в кейсе портала'],
	['7 сек', 'среднее время ответа ИИ-ассистента'],
];

const process = [
	['filter', 'Аудит', 'Фиксируем цели, процессы, роли и точки потерь.'],
	['layers', 'Проектирование', 'Описываем воронки, карточки, интеграции и этапы запуска.'],
	['settings', 'Настройка', 'Конфигурируем CRM, права, роботов, формы и каналы связи.'],
	['rocket', 'Запуск', 'Переносим данные, тестируем сценарии и обучаем сотрудников.'],
	['bar-chart', 'Развитие', 'Донастраиваем процессы и развиваем систему по KPI.'],
];

const packages = [
	{
		name: 'Аудит и дорожная карта',
		price: 'от 19 000 ₽',
		tag: 'Старт',
		text: 'Когда нужно понять архитектуру внедрения и бюджет.',
		items: ['интервью и разбор процессов', 'карта автоматизации', 'этапы и смета'],
		icon: 'filter',
	},
	{
		name: 'Быстрый старт CRM',
		price: 'от 39 000 ₽',
		tag: 'Популярно',
		text: 'Для малого бизнеса и первого запуска Bitrix24.',
		items: ['портал, роли и права', '1-2 воронки', 'почта или форма'],
		icon: 'rocket',
		featured: true,
	},
	{
		name: 'Комплексное внедрение',
		price: 'от 149 000 ₽',
		tag: 'Процессы',
		text: 'Для продаж, согласований, интеграций и аналитики.',
		items: ['бизнес-анализ', 'смарт-процессы', 'интеграции с 1С'],
		icon: 'workflow',
	},
];

const comparison = [
	['Скорость запуска', '2-5 дней', 'от 4 недель'],
	['Кастомизация', 'типовая логика', 'глубокие доработки'],
	['Инфраструктура', 'на стороне сервиса', 'под контролем компании'],
	['Подходит для', 'быстрого старта продаж', 'сложных требований безопасности'],
];

const faqs = [
	[
		'Сколько стоит внедрение Bitrix24?',
		'Базовый запуск начинается от 39 000 ₽. Итог зависит от числа пользователей, логики процессов, интеграций и объема миграции данных.',
	],
	[
		'Можно ли интегрировать с 1С, телефонией и сайтом?',
		'Да. Связываем Bitrix24 с 1С, телефонией, формами, почтой, сайтом, аналитикой и внешними сервисами.',
	],
	[
		'Что выбрать: облако или коробку?',
		'Облако подходит для быстрого старта и типовых задач. Коробка нужна для глубокой кастомизации, контроля инфраструктуры и требований безопасности.',
	],
];

const bitrixProblems = [
	{
		icon: 'message',
		title: 'Единый прием заявок',
		text: 'Заявки с сайта, почты и мессенджеров собираются в одном месте',
	},
	{
		icon: 'filter',
		title: 'Лиды не теряются',
		text: 'Ни один лид не теряется и не забывается',
	},
	{
		icon: 'workflow',
		title: 'Единый сценарий работы',
		text: 'Менеджеры работают по единому сценарию, а не кто как привык',
	},
	{
		icon: 'bar-chart',
		title: 'Прозрачная воронка',
		text: 'Руководитель видит прозрачную воронку и реальную загрузку команды',
	},
	{
		icon: 'file',
		title: 'Автоматические документы',
		text: 'Счета, КП, акты, напоминания и уведомления формируются автоматически',
	},
	{
		icon: 'phone',
		title: 'Связанные каналы',
		text: 'Телефония, 1С, сайт, формы, чат-боты и реклама связываются в единую систему',
	},
];

const bitrixPackages = [
	{
		name: 'Аудит и дорожная карта',
		price: 'от 19 000 ₽',
		icon: 'filter',
		tag: 'Старт',
		text: 'Когда нужно понять, что именно настраивать и сколько это будет стоить.',
		items: ['интервью с заказчиком', 'разбор текущих процессов', 'карта автоматизации', 'смета и этапы запуска'],
	},
	{
		name: 'Быстрый старт CRM',
		price: 'от 39 000 ₽',
		icon: 'rocket',
		tag: 'Старт',
		text: 'Для малого бизнеса и первого запуска Bitrix24.',
		items: ['настройка портала', 'пользователи, роли и права', 'базовая CRM', '1-2 воронки', 'обучение ответственного'],
	},
	{
		name: 'Отдел продаж под ключ',
		price: 'от 89 000 ₽',
		icon: 'users',
		tag: 'Продажи',
		text: 'Когда нужен управляемый отдел продаж, а не просто поставить CRM.',
		items: ['аудит продаж', 'воронки и карточки', 'роботы и автоматизация', 'отчеты для руководителя'],
	},
	{
		name: 'Комплексное внедрение',
		price: 'от 149 000 ₽',
		icon: 'workflow',
		tag: 'Популярно',
		text: 'Для продаж, внутренних процессов, согласований, интеграций и аналитики.',
		featured: true,
		items: ['бизнес-анализ', 'CRM, задачи и процессы', 'смарт-процессы', 'интеграции с 1С', 'запуск и сопровождение'],
	},
	{
		name: 'Коробка и интеграции',
		price: 'от 249 000 ₽',
		icon: 'shield',
		tag: 'Enterprise',
		text: 'Для требований к безопасности, инфраструктуре, кастомизации и интеграциям.',
		items: ['архитектура внедрения', 'коробочная версия', 'API-интеграции', 'нестандартные модули'],
	},
];

const bitrixAuditPackage = bitrixPackages[0];
const bitrixImplementationPackages = bitrixPackages.slice(1);

const bitrixServices = [
	{
		title: 'CRM и продажи',
		icon: 'briefcase',
		items: ['воронки продаж под направления бизнеса', 'карточки лидов, сделок и компаний', 'повторные продажи и клиентская база'],
	},
	{
		title: 'Автоматизация процессов',
		icon: 'workflow',
		items: ['согласования и маршруты заявок', 'отпуска, закупки, счета и заявки', 'смарт-процессы под нестандартные сценарии'],
	},
	{
		title: 'Интеграции',
		icon: 'layers',
		items: ['1С', 'сайт и интернет-магазин', 'телефония', 'WhatsApp, Telegram, email и чаты'],
	},
	{
		title: 'Индивидуальные доработки',
		icon: 'settings',
		items: ['кастомные модули', 'нестандартные отчеты', 'API-интеграции', 'сложные роли доступа'],
		accent: true,
	},
];

const bitrixClientLogos = [
	{ name: 'Perola Sky', src: '/clients/client-perola.png' },
	{ name: 'William Grant & Sons Ltd.', src: '/clients/client-wgs.png' },
	{ name: 'Альфа-страхование', src: '/clients/client-alfa.png' },
	{ name: 'Донстрой', src: '/clients/client-donstroy.png' },
];

const serviceIcon = {
	'Разработка': 'layers',
	'Автоматизация': 'workflow',
	'Дополнительно': 'spark',
};
---

<Layout
	title="DS2 — новая дизайн-система IT Time Now"
	description="Новая страница дизайн-системы IT Time Now: типографика, кнопки, формы, карточки и продающие секции."
	headerVariant="glass"
>
	<div class="ds2">
		<section class="ds2-hero">
			<div class="ds2-container grid gap-10 py-16 sm:py-20 lg:grid-cols-[1fr_0.86fr] lg:items-center">
				<div>
					<div class="ds2-kicker">DS2 / Dimensional Interface Schema</div>
					<h1 class="mt-5 max-w-5xl text-[44px] leading-none sm:text-[64px] lg:text-[72px]">
						Новая дизайн-система для продающих страниц IT Time Now
					</h1>
					<p class="mt-6 max-w-3xl text-lg leading-8 text-[color:var(--ds2-muted)] sm:text-xl">
						Страница-библиотека, откуда можно брать готовые элементы: текст, заголовки, плашки,
						кнопки, формы, карточки, блоки услуг, тарифы, кейсы, FAQ и CTA.
					</p>
					<div class="mt-8 flex flex-wrap gap-3">
						<a class="ds2-btn ds2-btn-primary" href="#actions">
							<span>Компоненты</span>
							<UiIcon name="arrow" class="h-4 w-4" />
						</a>
						<a class="ds2-btn ds2-btn-secondary" href="#sections">
							<span>Продающие блоки</span>
							<UiIcon name="layers" class="h-4 w-4" />
						</a>
					</div>
					<nav class="mt-8 flex flex-wrap gap-2" aria-label="Разделы DS2">
						{navigation.map(([title, href]) => (
							<a class="ds2-nav-pill" href={href}>{title}</a>
						))}
					</nav>
				</div>

				<div class="ds2-dashboard" aria-label="Пример стеклянного интерфейсного блока">
					<div class="flex items-start justify-between gap-4">
						<div>
							<p class="text-sm text-[color:var(--ds2-muted)]">Воронка внедрения</p>
							<h2 class="mt-1 text-2xl">CRM + AI + интеграции</h2>
						</div>
						<span class="ds2-status ds2-status-success">готово</span>
					</div>
					<div class="mt-6 grid gap-3 sm:grid-cols-3">
						{stats.slice(0, 3).map(([value, label]) => (
							<div class="ds2-metric">
								<p>{value}</p>
								<span>{label}</span>
							</div>
						))}
					</div>
					<div class="mt-6 grid gap-3">
						{[
							['Новая заявка', '82%'],
							['Квалификация', '64%'],
							['КП и договор', '38%'],
							['Запуск', '24%'],
						].map(([label, value]) => (
							<div>
								<div class="mb-2 flex justify-between text-sm">
									<span>{label}</span>
									<span class="font-bold text-[color:var(--ds2-primary)]">{value}</span>
								</div>
								<div class="ds2-progress"><span style={`width:${value}`}></span></div>
							</div>
						))}
					</div>
					<div class="mt-6 flex flex-wrap gap-2">
						{['Bitrix24', '1С', 'OpenAI', 'FastAPI'].map((item) => (
							<span class="ds2-chip">{item}</span>
						))}
					</div>
				</div>
			</div>
		</section>

		<section id="tokens" class="ds2-section">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">01 / Tokens</p>
						<h2>Цвет, сетка, радиусы и материал</h2>
					</div>
					<p>
						Основа взята из Dimensional Interface Schema: электрический синий, стеклянные панели,
						сильная сетка, 4px-ритм и контролируемая глубина.
					</p>
				</div>

				<div class="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">
					{palette.map((color) => (
						<article class="ds2-card ds2-token-card">
							<div class="ds2-swatch" style={`background:${color.value}`}></div>
							<p class="mt-5 text-lg">{color.name}</p>
							<p class="mt-1 font-mono text-sm text-[color:var(--ds2-primary)]">{color.value}</p>
							<p class="mt-3 text-sm leading-6 text-[color:var(--ds2-muted)]">{color.usage}</p>
						</article>
					))}
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-[1fr_0.7fr]">
					<div class="ds2-card">
						<div class="flex flex-wrap items-end justify-between gap-5">
							<div>
								<p class="ds2-kicker">Spacing</p>
								<h3 class="mt-2 text-2xl">4px-ритм</h3>
							</div>
							<p class="max-w-md text-sm leading-6 text-[color:var(--ds2-muted)]">
								Шкала подходит для компактных интерфейсов, больших лендинговых секций и плотных
								коммерческих карточек.
							</p>
						</div>
						<div class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
							{spacing.map((item) => (
								<div class="ds2-scale-row">
									<span style={`width:${Number(item) * 2}px`}></span>
									<b>{item}px</b>
								</div>
							))}
						</div>
					</div>

					<div class="ds2-card">
						<p class="ds2-kicker">Radius</p>
						<h3 class="mt-2 text-2xl">Семейство углов</h3>
						<div class="mt-6 grid grid-cols-3 gap-3">
							{radii.map((item) => (
								<div class="ds2-radius-sample" style={`border-radius:${item}px`}>
									{item === '9999' ? 'pill' : `${item}px`}
								</div>
							))}
						</div>
					</div>
				</div>
			</div>
		</section>

		<section id="typography" class="ds2-section ds2-section-soft">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">02 / Typography</p>
						<h2>Текстовые стили и плашки</h2>
					</div>
					<p>
						Готовые размеры для первого экрана, секций, карточек, лид-абзацев, обычного текста,
						цитат, списков и метаданных.
					</p>
				</div>

				<div class="mt-10 grid gap-5">
					{typography.map((item) => (
						<div class="ds2-type-row">
							<div>
								<p class="ds2-type-name">{item.name}</p>
								<p class="mt-2 text-[color:var(--ds2-muted)]">{item.note}</p>
							</div>
							<p
								class="ds2-type-example"
								style={`--type-size:${item.size};--type-line:${item.leading};--type-weight:${item.weight};font-size:${item.size};line-height:${item.leading};font-weight:${item.weight}`}
							>
								{item.text}
							</p>
						</div>
					))}
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-[1fr_0.72fr]">
					<article class="ds2-card">
						<p class="ds2-kicker">Editorial</p>
						<h3 class="mt-3 max-w-3xl text-[36px] leading-[1.05]">
							От прототипа до промышленного внедрения
						</h3>
						<p class="mt-5 max-w-3xl text-lg leading-8 text-[color:var(--ds2-muted)]">
							Подбираем стек под задачу, а не задачу под стек. Разрабатываем сайты и сервисы,
							внедряем CRM и ERP, обучаем ИИ-ассистентов и сопровождаем IT-инфраструктуру.
						</p>
						<ul class="mt-6 grid gap-3 text-[color:var(--ds2-ink)] sm:grid-cols-2">
							{[
								'одна мысль на строку',
								'понятный следующий шаг',
								'цифры рядом с результатом',
								'ссылки подсвечены основным синим',
							].map((item) => (
								<li class="flex gap-3">
									<UiIcon name="check" class="mt-1 h-4 w-4 shrink-0 text-[color:var(--ds2-primary)]" />
									<span>{item}</span>
								</li>
							))}
						</ul>
					</article>

					<article class="ds2-quote">
						<p class="ds2-kicker">Quote</p>
						<blockquote class="mt-4 text-2xl leading-9">
							“Сначала фиксируем бизнес-задачу и границы этапа, потом настраиваем инструменты.”
						</blockquote>
						<p class="mt-5 text-sm leading-6 text-[color:var(--ds2-muted)]">
							Плашка для принципов, отзывов, важных ограничений и коротких экспертных выводов.
						</p>
					</article>
				</div>

				<div class="mt-5 grid gap-4 md:grid-cols-3">
					{callouts.map((item) => (
						<article class={`ds2-callout ds2-callout-${item.type}`}>
							<p>{item.title}</p>
							<span>{item.text}</span>
						</article>
					))}
				</div>
			</div>
		</section>

		<section id="actions" class="ds2-section">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">03 / Actions</p>
						<h2>Кнопки, бейджи, табы и навигация</h2>
					</div>
					<p>
						Кнопки держат pill-геометрию из исходной схемы. Ссылки и табы используют синий как
						активное состояние, а не как декоративный шум.
					</p>
				</div>

				<div class="mt-10 grid gap-5 lg:grid-cols-[1fr_0.82fr]">
					<div class="ds2-card">
						<p class="ds2-kicker">Buttons</p>
						<div class="mt-5 flex flex-wrap gap-3">
							{actionButtons.map((button) => (
								<a class={`ds2-btn ds2-btn-${button.variant}`} href="#forms">
									<span>{button.label}</span>
									<UiIcon name={button.icon} class="h-4 w-4" />
								</a>
							))}
						</div>
						<div class="ds2-free-action">
							<a class="ds2-btn ds2-btn-primary" href="#forms">
								<span>Получить консультацию</span>
								<UiIcon name="arrow" class="h-4 w-4" />
							</a>
							<p class="ds2-free-label">Бесплатно!</p>
						</div>
						<div class="mt-6 flex flex-wrap gap-3">
							<button class="ds2-icon-button" type="button" aria-label="Фильтр">
								<span class="ds2-icon-main"><UiIcon name="filter" class="h-4 w-4" /></span>
								<span class="ds2-icon-clone" aria-hidden="true"><UiIcon name="filter" class="h-4 w-4" /></span>
							</button>
							<button class="ds2-icon-button is-active" type="button" aria-label="Слои">
								<span class="ds2-icon-main"><UiIcon name="layers" class="h-4 w-4" /></span>
								<span class="ds2-icon-clone" aria-hidden="true"><UiIcon name="layers" class="h-4 w-4" /></span>
							</button>
							<button class="ds2-icon-button" type="button" aria-label="Настройки">
								<span class="ds2-icon-main"><UiIcon name="settings" class="h-4 w-4" /></span>
								<span class="ds2-icon-clone" aria-hidden="true"><UiIcon name="settings" class="h-4 w-4" /></span>
							</button>
							<button class="ds2-icon-button" type="button" aria-label="Сообщение">
								<span class="ds2-icon-main"><UiIcon name="message" class="h-4 w-4" /></span>
								<span class="ds2-icon-clone" aria-hidden="true"><UiIcon name="message" class="h-4 w-4" /></span>
							</button>
						</div>
					</div>

					<div class="ds2-card">
						<p class="ds2-kicker">Badges</p>
						<div class="mt-5 flex flex-wrap gap-2">
							{chips.map((item) => <span class="ds2-chip">{item}</span>)}
						</div>
						<div class="mt-6 flex flex-wrap gap-2">
							<span class="ds2-status ds2-status-success">в работе</span>
							<span class="ds2-status ds2-status-info">аудит</span>
							<span class="ds2-status ds2-status-warning">оценка</span>
						</div>
					</div>
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-2">
					<div class="ds2-card">
						<p class="ds2-kicker">Tabs</p>
						<div class="mt-5 flex flex-wrap gap-2">
							<button class="ds2-tab is-active" type="button">Продажи</button>
							<button class="ds2-tab" type="button">Интеграции</button>
							<button class="ds2-tab" type="button">Документы</button>
							<button class="ds2-tab" type="button">Поддержка</button>
						</div>
					</div>
					<div class="ds2-card">
						<p class="ds2-kicker">Breadcrumbs / Pagination</p>
						<div class="mt-5 flex flex-wrap items-center gap-2 text-sm text-[color:var(--ds2-muted)]">
							<a href="/" class="hover:text-[color:var(--ds2-primary)]">Главная</a>
							<UiIcon name="chevron" class="h-4 w-4" />
							<a href="/services" class="hover:text-[color:var(--ds2-primary)]">Услуги</a>
							<UiIcon name="chevron" class="h-4 w-4" />
							<span class="text-[color:var(--ds2-ink)]">Bitrix24</span>
						</div>
						<div class="mt-5 flex gap-2">
							<button class="ds2-page-button" type="button">
								<span class="ds2-page-main">1</span>
								<span class="ds2-page-clone" aria-hidden="true">1</span>
							</button>
							<button class="ds2-page-button is-active" type="button">
								<span class="ds2-page-main">2</span>
								<span class="ds2-page-clone" aria-hidden="true">2</span>
							</button>
							<button class="ds2-page-button" type="button">
								<span class="ds2-page-main">3</span>
								<span class="ds2-page-clone" aria-hidden="true">3</span>
							</button>
							<button class="ds2-page-button" type="button" aria-label="Следующая страница">
								<span class="ds2-page-main"><UiIcon name="arrow" class="h-4 w-4" /></span>
								<span class="ds2-page-clone" aria-hidden="true"><UiIcon name="arrow" class="h-4 w-4" /></span>
							</button>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section id="forms" class="ds2-section ds2-section-dark">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">04 / Forms</p>
						<h2>Формы заявки и поля</h2>
					</div>
					<p>
						Форма рассчитана на продающие страницы: короткие поля, ясный CTA, быстрый выбор
						интересующих услуг и аккуратная юридическая строка.
					</p>
				</div>

				<div class="mt-10 grid gap-6 lg:grid-cols-[0.8fr_1fr]">
					<div>
						<h3 class="text-3xl leading-tight">Получите план внедрения и предварительную смету</h3>
						<p class="mt-5 leading-8 text-white/72">
							Расскажите, что хотите автоматизировать: продажи, заявки с сайта, документооборот,
							согласования, задачи, производство, сервис или интеграции с 1С.
						</p>
						<div class="mt-8 grid gap-3 sm:grid-cols-2">
							{[
								['users', 'команда и роли'],
								['database', 'перенос базы'],
								['phone', 'телефония и сайт'],
								['shield', 'SLA и безопасность'],
							].map(([icon, text]) => (
								<div class="ds2-dark-pill">
									<UiIcon name={icon} class="h-4 w-4" />
									{text}
								</div>
							))}
						</div>
					</div>

					<form class="ds2-form" method="post" action="#">
						<div class="grid gap-4 sm:grid-cols-2">
							<label>
								<span>Имя</span>
								<input class="ds2-input" required type="text" name="name" placeholder="Анна" />
							</label>
							<label>
								<span>Телефон или email</span>
								<input class="ds2-input" required type="text" name="contact" placeholder="+7 000 000-00-00" />
							</label>
						</div>
						<div class="ds2-form-field">
							<span id="ds2-form-service-label" class="ds2-form-label">Услуга</span>
							<div class="ds2-select ds2-select-form" data-ds2-select>
								<input type="hidden" name="service" value={selectOptions[0]} data-ds2-select-input />
								<button
									class="ds2-select-trigger ds2-select-trigger-lg"
									type="button"
									aria-haspopup="listbox"
									aria-expanded="false"
									aria-labelledby="ds2-form-service-label"
								>
									<span class="ds2-select-value">{selectOptions[0]}</span>
									<UiIcon name="chevron" class="ds2-select-chevron h-4 w-4" />
								</button>
								<div class="ds2-select-content" role="listbox" aria-labelledby="ds2-form-service-label">
									<button class="ds2-select-scroll ds2-select-scroll-up" type="button" tabindex="-1" aria-hidden="true">
										<UiIcon name="chevron" class="h-4 w-4" />
									</button>
									<div class="ds2-select-viewport">
										<p class="ds2-select-label">Выберите услугу</p>
										{selectOptions.map((option, index) => (
											<button
												class:list={['ds2-select-item', index === 0 && 'is-selected']}
												type="button"
												role="option"
												aria-selected={index === 0 ? 'true' : 'false'}
												data-value={option}
											>
												<span class="ds2-select-indicator"><UiIcon name="check" class="h-4 w-4" /></span>
												<span>{option}</span>
											</button>
										))}
									</div>
									<button class="ds2-select-scroll ds2-select-scroll-down" type="button" tabindex="-1" aria-hidden="true">
										<UiIcon name="chevron" class="h-4 w-4" />
									</button>
								</div>
							</div>
						</div>
						<label>
							<span>Коротко о задаче</span>
							<textarea class="ds2-input" name="message" rows="4" placeholder="Нужно связать CRM, сайт, телефонию и 1С"></textarea>
						</label>
						<div class="grid gap-3 sm:grid-cols-2">
							<label class="ds2-check"><input type="checkbox" checked /> CRM и воронки</label>
							<label class="ds2-check"><input type="checkbox" /> Интеграции</label>
							<label class="ds2-check"><input type="checkbox" /> ИИ-ассистент</label>
							<label class="ds2-check"><input type="checkbox" /> Поддержка SLA</label>
						</div>
						<div class="grid gap-4 sm:grid-cols-[1fr_auto] sm:items-center">
							<label class="ds2-toggle"><input type="checkbox" checked /><span></span>Нужен NDA</label>
							<button class="ds2-btn ds2-btn-primary" type="submit">
								<span>Отправить заявку</span>
								<UiIcon name="arrow" class="h-4 w-4" />
							</button>
						</div>
						<p class="text-xs leading-5 text-[color:var(--ds2-muted)]">
							Отправляя форму, вы соглашаетесь с обработкой персональных данных.
						</p>
					</form>
				</div>

				<div class="mt-6 grid gap-6 lg:grid-cols-[0.8fr_1fr]">
					<article class="ds2-select-note">
						<p class="ds2-kicker">Select</p>
						<h3>Выпадающий список с анимацией</h3>
						<p>
							Компонент повторяет структуру Radix Select: trigger, value, content, viewport,
							label, item, indicator, separator и scroll controls. Анимация раскрытия использует
							fade, zoom и slide from top.
						</p>
					</article>

					<div class="ds2-select-lab">
						<div class="ds2-select" data-ds2-select>
							<button
								class="ds2-select-trigger ds2-select-trigger-lg"
								type="button"
								aria-haspopup="listbox"
								aria-expanded="false"
							>
								<span class="ds2-select-value">{selectOptions[0]}</span>
								<UiIcon name="chevron" class="ds2-select-chevron h-4 w-4" />
							</button>
							<div class="ds2-select-content" role="listbox">
								<button class="ds2-select-scroll ds2-select-scroll-up" type="button" tabindex="-1" aria-hidden="true">
									<UiIcon name="chevron" class="h-4 w-4" />
								</button>
								<div class="ds2-select-viewport">
									<p class="ds2-select-label">Услуги</p>
									{selectOptions.slice(0, 3).map((option, index) => (
										<button
											class:list={['ds2-select-item', index === 0 && 'is-selected']}
											type="button"
											role="option"
											aria-selected={index === 0 ? 'true' : 'false'}
											data-value={option}
										>
											<span class="ds2-select-indicator"><UiIcon name="check" class="h-4 w-4" /></span>
											<span>{option}</span>
										</button>
									))}
									<div class="ds2-select-separator"></div>
									<p class="ds2-select-label">Дополнительно</p>
									{selectOptions.slice(3).map((option) => (
										<button
											class="ds2-select-item"
											type="button"
											role="option"
											aria-selected="false"
											data-value={option}
										>
											<span class="ds2-select-indicator"><UiIcon name="check" class="h-4 w-4" /></span>
											<span>{option}</span>
										</button>
									))}
								</div>
								<button class="ds2-select-scroll ds2-select-scroll-down" type="button" tabindex="-1" aria-hidden="true">
									<UiIcon name="chevron" class="h-4 w-4" />
								</button>
							</div>
						</div>

						<div class="mt-4 grid gap-3 sm:grid-cols-3">
							<button class="ds2-select-trigger ds2-select-trigger-sm" type="button">
								<span>Small</span>
								<UiIcon name="chevron" class="ds2-select-chevron h-4 w-4" />
							</button>
							<button class="ds2-select-trigger" type="button">
								<span>Medium</span>
								<UiIcon name="chevron" class="ds2-select-chevron h-4 w-4" />
							</button>
							<button class="ds2-select-trigger ds2-select-trigger-lg" type="button">
								<span>Large</span>
								<UiIcon name="chevron" class="ds2-select-chevron h-4 w-4" />
							</button>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section id="sections" class="ds2-section">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">05 / Selling Sections</p>
						<h2>Готовые блоки для продающего сайта</h2>
					</div>
					<p>
						Секции собраны на контенте проекта ITTN: услуги, кейсы, цифры, процесс, тарифы,
						сравнение, отзывы, FAQ и финальный призыв.
					</p>
				</div>

				<div class="mt-10 grid gap-5">
					<section class="ds2-mini-hero">
						<div>
							<p class="ds2-kicker">Hero block</p>
							<h3>Цифровые системы эффективного бизнеса</h3>
							<p>
								Разрабатываем сайты и сервисы, внедряем CRM и ERP, обучаем ИИ-ассистентов и
								сопровождаем IT-инфраструктуру под ключ.
							</p>
							<div class="mt-6 flex flex-wrap gap-3">
								<a class="ds2-btn ds2-btn-primary" href="#sales"><span>Получить консультацию</span></a>
								<a class="ds2-btn ds2-btn-secondary" href="/cases"><span>Смотреть кейсы</span></a>
							</div>
						</div>
						<div class="ds2-mini-visual">
							<div class="ds2-mini-line"></div>
							<div class="ds2-mini-line is-short"></div>
							<div class="grid grid-cols-2 gap-3">
								<div class="ds2-mini-tile">CRM</div>
								<div class="ds2-mini-tile">AI</div>
								<div class="ds2-mini-tile">ERP</div>
								<div class="ds2-mini-tile">Web</div>
							</div>
						</div>
					</section>

					<section class="ds2-logo-strip" aria-label="Пример полосы клиентов">
						{['ВкусВилл', 'Россельхозбанк', 'Северсталь', 'АльфаСтрахование', 'Donstroy'].map((item) => (
							<span>{item}</span>
						))}
					</section>

					<section class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
						{services.slice(0, 6).map((service) => (
							<a class="ds2-service-card" href={`/services/${service.id}/`}>
								<div class="ds2-service-icon">
									<UiIcon name={serviceIcon[service.data.group] ?? 'layers'} class="h-5 w-5" />
								</div>
								<p class="mt-5 text-sm text-[color:var(--ds2-primary)]">{service.data.group}</p>
								<h3 class="mt-2 text-2xl">{service.data.title}</h3>
								<p class="mt-3 text-sm leading-6 text-[color:var(--ds2-muted)]">{service.data.summary}</p>
							</a>
						))}
					</section>

					<section class="grid gap-4 md:grid-cols-4">
						{stats.map(([value, label]) => (
							<div class="ds2-stat-card">
								<p>{value}</p>
								<span>{label}</span>
							</div>
						))}
					</section>
				</div>
			</div>
		</section>

		<section id="sales" class="ds2-section ds2-section-soft">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">06 / Conversion Blocks</p>
						<h2>Тарифы, кейсы, сравнение и FAQ</h2>
					</div>
					<p>
						Эти блоки закрывают типовые вопросы перед заявкой: цена, состав работ, доверие,
						выбор решения и сценарий внедрения.
					</p>
				</div>

				<div class="mt-10 grid gap-5 lg:grid-cols-3">
					{packages.map((pack) => (
						<article class:list={['ds2-price-card', pack.featured && 'is-featured']}>
							<div class="flex items-start justify-between gap-4">
								<div class="ds2-service-icon">
									<UiIcon name={pack.icon} class="h-5 w-5" />
								</div>
								<span class="ds2-chip">{pack.tag}</span>
							</div>
							<h3 class="mt-6 text-2xl">{pack.name}</h3>
							<p class="mt-3 text-3xl text-[color:var(--ds2-primary)]">{pack.price}</p>
							<p class="mt-3 text-sm leading-6 text-[color:var(--ds2-muted)]">{pack.text}</p>
							<ul class="mt-6 grid gap-3 text-sm">
								{pack.items.map((item) => (
									<li class="flex gap-2">
										<UiIcon name="check" class="mt-0.5 h-4 w-4 shrink-0 text-[color:var(--ds2-success)]" />
										<span>{item}</span>
									</li>
								))}
							</ul>
							<a class="ds2-btn ds2-btn-secondary mt-7 w-full" href="#forms"><span>Рассчитать проект</span></a>
						</article>
					))}
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-[0.72fr_1fr]">
					<section class="ds2-card">
						<p class="ds2-kicker">Process</p>
						<h3 class="mt-2 text-3xl">Пять этапов внедрения</h3>
						<div class="mt-6 grid gap-3">
							{process.map(([icon, title, text], index) => (
								<div class="ds2-step">
									<div class="ds2-step-number">{String(index + 1).padStart(2, '0')}</div>
									<UiIcon name={icon} class="h-5 w-5 text-[color:var(--ds2-primary)]" />
									<div>
										<p>{title}</p>
										<span>{text}</span>
									</div>
								</div>
							))}
						</div>
					</section>

					<section class="ds2-card">
						<p class="ds2-kicker">Cases</p>
						<h3 class="mt-2 text-3xl">Кейсы как карточки доверия</h3>
						<div class="mt-6 grid gap-4">
							{cases.map((item) => (
								<a class="ds2-case-row" href={`/cases/${item.id}/`}>
									<div>
										<p class="text-sm text-[color:var(--ds2-muted)]">
											{item.data.client}
											{item.data.industry && ` / ${item.data.industry}`}
											{item.data.year && ` / ${item.data.year}`}
										</p>
										<h4 class="mt-1 text-xl">{item.data.title}</h4>
										<p class="mt-2 text-sm leading-6 text-[color:var(--ds2-muted)]">{item.data.summary}</p>
									</div>
									<UiIcon name="arrow" class="h-5 w-5 shrink-0 text-[color:var(--ds2-primary)]" />
								</a>
							))}
						</div>
					</section>
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-[1fr_0.78fr]">
					<section class="ds2-card">
						<p class="ds2-kicker">Comparison</p>
						<h3 class="mt-2 text-3xl">Облако или коробка</h3>
						<div class="mt-6 overflow-x-auto">
							<table class="ds2-table">
								<thead>
									<tr>
										<th>Критерий</th>
										<th>Облако</th>
										<th>Коробка</th>
									</tr>
								</thead>
								<tbody>
									{comparison.map(([name, cloud, box]) => (
										<tr>
											<td>{name}</td>
											<td>{cloud}</td>
											<td>{box}</td>
										</tr>
									))}
								</tbody>
							</table>
						</div>
					</section>

					<section class="ds2-testimonial">
						<p class="ds2-kicker">Testimonial</p>
						<blockquote>
							“Время оформления полиса сократилось в 3 раза, а 800+ агентов работают в новом
							портале каждый день.”
						</blockquote>
						<div class="mt-6 flex items-center gap-3">
							<div class="ds2-avatar">АС</div>
							<div>
								<p>АльфаСтрахование</p>
								<span>B2B-портал для страховых агентов</span>
							</div>
						</div>
					</section>
				</div>

				<section class="mt-5 grid gap-4 lg:grid-cols-[0.5fr_1fr]">
					<div>
						<p class="ds2-kicker">FAQ</p>
						<h3 class="mt-2 text-3xl">Частые вопросы</h3>
					</div>
					<div class="grid gap-3">
						{faqs.map(([question, answer]) => (
							<details class="ds2-faq">
								<summary>
									<span>{question}</span>
									<UiIcon name="chevron" class="h-4 w-4" />
								</summary>
								<div class="ds2-faq-content">
									<p>{answer}</p>
								</div>
							</details>
						))}
					</div>
				</section>
			</div>
		</section>

		<section id="bitrix24" class="ds2-section">
			<div class="ds2-container">
				<div class="ds2-section-head bitrix-wide-head">
					<div>
						<p class="ds2-kicker">07 / Bitrix24 Patterns</p>
						<h2>Обновленные паттерны со страницы внедрения Bitrix24</h2>
					</div>
					<p>
						На странице услуги появились более широкие коммерческие блоки: центрированные карточки
						проблем, тарифная сетка с аудитом, горизонтальные мобильные списки, таймлайн проекта,
						логотипы клиентов и темная карточка доверия.
					</p>
				</div>

				<div class="bitrix-problems">
					{bitrixProblems.map(({ icon, title, text }) => (
						<article class="ds2-service-card bitrix-problem-card">
							<div class="ds2-service-icon">
								<UiIcon name={icon} class="h-5 w-5" />
							</div>
							<p>{title}</p>
							<h3>{text}</h3>
						</article>
					))}
				</div>

				<div class="bitrix-prices">
					<article class="ds2-price-card bitrix-audit-card">
						<div class="bitrix-card-top">
							<div class="ds2-service-icon">
								<UiIcon name={bitrixAuditPackage.icon} class="h-5 w-5" />
							</div>
							<span class="ds2-chip">{bitrixAuditPackage.tag}</span>
						</div>
						<div class="bitrix-audit-body">
							<div>
								<h3>{bitrixAuditPackage.name}</h3>
								<p class="bitrix-card-text">{bitrixAuditPackage.text}</p>
							</div>
							<p class="bitrix-price">{bitrixAuditPackage.price}</p>
						</div>
						<ul>
							{bitrixAuditPackage.items.map((item) => (
								<li>
									<UiIcon name="check" class="h-4 w-4" />
									<span>{item}</span>
								</li>
							))}
						</ul>
						<a class="ds2-btn ds2-btn-secondary" href="#forms">
							<span>Заказать аудит</span>
						</a>
					</article>

					{bitrixImplementationPackages.map((pack) => (
						<article class:list={['ds2-price-card', pack.featured && 'is-featured']}>
							<div class="bitrix-card-top">
								<div class="ds2-service-icon">
									<UiIcon name={pack.icon} class="h-5 w-5" />
								</div>
								<span class="ds2-chip">{pack.tag}</span>
							</div>
							<h3>{pack.name}</h3>
							<p class="bitrix-price">{pack.price}</p>
							<p class="bitrix-card-text">{pack.text}</p>
							<ul>
								{pack.items.map((item) => (
									<li>
										<UiIcon name="check" class="h-4 w-4" />
										<span>{item}</span>
									</li>
								))}
							</ul>
							<a class="ds2-btn ds2-btn-secondary" href="#forms">
								<span>Рассчитать</span>
							</a>
						</article>
					))}
				</div>

				<div class="mt-5 grid gap-5 lg:grid-cols-[0.72fr_1fr]">
					<section class="ds2-card bitrix-pattern-copy">
						<p class="ds2-kicker">Project Timeline</p>
						<h3>Пять этапов управляемого внедрения</h3>
						<p>
							Паттерн подходит для услуг, где важно показать контроль: этап, действие,
							результат и следующий шаг без перегруженной схемы.
						</p>
						<a class="ds2-btn ds2-btn-dark" href="#forms">
							<span>Собрать состав работ</span>
							<UiIcon name="arrow" class="h-4 w-4" />
						</a>
					</section>

					<ol class="bitrix-timeline" aria-label="Пример таймлайна внедрения">
						{process.map(([icon, title, text], index) => (
							<li class="bitrix-timeline-item">
								<div class="bitrix-timeline-date">
									<span>Этап</span>
									<time>{String(index + 1).padStart(2, '0')}</time>
								</div>
								<div class="bitrix-timeline-track" aria-hidden="true">
									<div class="bitrix-timeline-line"></div>
									<div class="bitrix-timeline-dot">
										<UiIcon name={icon} class="h-4 w-4" />
									</div>
								</div>
								<div class="bitrix-timeline-content">
									<h3>{title}</h3>
									<p>{text}</p>
								</div>
							</li>
						))}
					</ol>
				</div>

				<div class="bitrix-logo-marquee" aria-label="Логотипы клиентов">
					<div class="bitrix-logo-track">
						{[0, 1, 2].map((group) => (
							<div class="bitrix-logo-group" aria-hidden={group === 0 ? 'false' : 'true'}>
								{bitrixClientLogos.map((client) => (
									<figure class="bitrix-logo-card">
										<img src={client.src} alt={client.name} loading="lazy" decoding="async" />
									</figure>
								))}
							</div>
						))}
					</div>
				</div>

				<div class="bitrix-services">
					{bitrixServices.map((group) => (
						<article class:list={['ds2-service-card', group.accent && 'is-accent']}>
							<div class="ds2-service-icon">
								<UiIcon name={group.icon} class="h-5 w-5" />
							</div>
							<h3>{group.title}</h3>
							<ul>
								{group.items.map((item) => (
									<li>
										<UiIcon name="chevron" class="h-4 w-4" />
										<span>{item}</span>
									</li>
								))}
							</ul>
						</article>
					))}
				</div>

				<section class="ds2-testimonial bitrix-reasons-card mt-5">
					<div>
						<p class="ds2-kicker">Trust Card</p>
						<blockquote>
							Сначала фиксируем бизнес-задачу и границы этапа, потом настраиваем инструменты.
						</blockquote>
					</div>
					<div class="bitrix-reasons">
						{['не навязываем лишние модули', 'фиксируем этапы и результат', 'объясняем, что даст эффект', 'остаемся на связи после запуска'].map((item) => (
							<span><UiIcon name="check" class="h-4 w-4" />{item}</span>
						))}
					</div>
				</section>
			</div>
		</section>

		<section id="interface" class="ds2-section">
			<div class="ds2-container">
				<div class="ds2-section-head">
					<div>
						<p class="ds2-kicker">08 / Interface Pieces</p>
						<h2>Элементы для интерфейсных вставок</h2>
					</div>
					<p>
						Для современных продающих страниц часто нужны не только маркетинговые блоки, но и
						фрагменты продукта: KPI, статусы, таблицы, прогресс, уведомления и мини-дашборды.
					</p>
				</div>

				<div class="mt-10 grid gap-5 lg:grid-cols-[0.8fr_1fr]">
					<section class="ds2-card">
						<p class="ds2-kicker">Dashboard fragment</p>
						<div class="mt-5 grid gap-3">
							{[
								['Новые заявки', '128', '+18%'],
								['Сделки в работе', '47', '+9%'],
								['Документы на согласовании', '16', '-22%'],
							].map(([label, value, delta]) => (
								<div class="ds2-kpi-row">
									<div>
										<p>{label}</p>
										<span>{value}</span>
									</div>
									<b>{delta}</b>
								</div>
							))}
						</div>
						<div class="mt-5">
							<label class="text-sm text-[color:var(--ds2-muted)]" for="maturity">Готовность проекта</label>
							<input id="maturity" class="ds2-range mt-3" type="range" min="0" max="100" value="72" />
						</div>
					</section>

					<section class="ds2-card">
						<p class="ds2-kicker">Timeline</p>
						<div class="mt-6 grid gap-4">
							{[
								['09:30', 'Поступила заявка с сайта'],
								['10:05', 'Менеджер получил задачу и SLA'],
								['11:20', 'Отправлено КП по шаблону'],
								['12:10', 'Сделка перешла в согласование'],
							].map(([time, text]) => (
								<div class="ds2-timeline-row">
									<time>{time}</time>
									<span></span>
									<p>{text}</p>
								</div>
							))}
						</div>
					</section>
				</div>

				<section class="ds2-final-cta mt-5">
					<div>
						<p class="ds2-kicker">Final CTA</p>
						<h2>Соберите продающую страницу из DS2-блоков</h2>
						<p>
							Используйте эту страницу как витрину компонентов для новых страниц ITTN: услуги,
							кейсы, посадочные страницы, промо для Bitrix24, AI и разработки.
						</p>
					</div>
					<a class="ds2-btn ds2-btn-primary" href="#tokens">
						<span>Вернуться к токенам</span>
						<UiIcon name="arrow" class="h-4 w-4" />
					</a>
				</section>
			</div>
		</section>
	</div>
</Layout>

<script>
	function bindDs2Faq() {
		document.querySelectorAll<HTMLDetailsElement>('.ds2-faq').forEach((details) => {
			if (details.dataset.faqBound) return;
			details.dataset.faqBound = '1';

			const summary = details.querySelector('summary');
			const content = details.querySelector<HTMLElement>('.ds2-faq-content');
			if (!summary || !content) return;

			summary.addEventListener('click', (event) => {
				event.preventDefault();

				if (details.dataset.animating === '1') return;
				details.dataset.animating = '1';

				const duration = 220;
				const easing = 'ease-out';

				if (details.open) {
					const startHeight = `${content.offsetHeight}px`;
					details.classList.add('is-closing');
					content.style.height = startHeight;
					content.style.overflow = 'hidden';

					const animation = content.animate(
						[
							{ height: startHeight, opacity: 1, transform: 'translateY(0)' },
							{ height: '0px', opacity: 0, transform: 'translateY(-4px)' },
						],
						{ duration, easing: 'ease-in' },
					);

					animation.onfinish = () => {
						details.open = false;
						details.classList.remove('is-closing');
						content.style.height = '';
						content.style.overflow = '';
						delete details.dataset.animating;
					};
					animation.oncancel = () => {
						details.classList.remove('is-closing');
						content.style.height = '';
						content.style.overflow = '';
						delete details.dataset.animating;
					};
					return;
				}

				details.open = true;
				const endHeight = `${content.offsetHeight}px`;
				content.style.height = '0px';
				content.style.overflow = 'hidden';

				const animation = content.animate(
					[
						{ height: '0px', opacity: 0, transform: 'translateY(-4px)' },
						{ height: endHeight, opacity: 1, transform: 'translateY(0)' },
					],
					{ duration, easing },
				);

				animation.onfinish = () => {
					content.style.height = '';
					content.style.overflow = '';
					delete details.dataset.animating;
				};
				animation.oncancel = () => {
					content.style.height = '';
					content.style.overflow = '';
					delete details.dataset.animating;
				};
			});
		});
	}

	function bindDs2Select() {
		document.querySelectorAll<HTMLElement>('[data-ds2-select]').forEach((select) => {
			if (select.dataset.selectBound) return;
			select.dataset.selectBound = '1';

			const trigger = select.querySelector<HTMLButtonElement>('.ds2-select-trigger');
			const value = select.querySelector<HTMLElement>('.ds2-select-value');
			const input = select.querySelector<HTMLInputElement>('[data-ds2-select-input]');
			const items = select.querySelectorAll<HTMLButtonElement>('.ds2-select-item');

			const close = () => {
				select.removeAttribute('data-open');
				trigger?.setAttribute('aria-expanded', 'false');
			};

			const open = () => {
				select.setAttribute('data-open', 'true');
				trigger?.setAttribute('aria-expanded', 'true');
			};

			trigger?.addEventListener('click', (event) => {
				event.stopPropagation();
				if (select.getAttribute('data-open') === 'true') {
					close();
					return;
				}
				open();
			});

			items.forEach((item) => {
				item.addEventListener('click', (event) => {
					event.stopPropagation();
					const nextValue = item.dataset.value ?? item.textContent?.trim() ?? '';
					if (value) value.textContent = nextValue;
					if (input) input.value = nextValue;

					items.forEach((currentItem) => {
						const isSelected = currentItem === item;
						currentItem.classList.toggle('is-selected', isSelected);
						currentItem.setAttribute('aria-selected', isSelected ? 'true' : 'false');
					});

					close();
				});
			});

			select.addEventListener('keydown', (event) => {
				if (event.key === 'Escape') close();
			});

			document.addEventListener('click', (event) => {
				const target = event.target;
				if (target instanceof Node && !select.contains(target)) close();
			});
		});
	}

	bindDs2Faq();
	bindDs2Select();
	document.addEventListener('astro:page-load', bindDs2Faq);
	document.addEventListener('astro:page-load', bindDs2Select);
</script>

<style>
	.ds2 {
		--ds2-primary: #232bff;
		--ds2-secondary: #5f66ff;
		--ds2-tertiary: #8b5cff;
		--ds2-bg: #ffffff;
		--ds2-soft: #f6f7fb;
		--ds2-card: rgba(255, 255, 255, 0.74);
		--ds2-card-strong: #ffffff;
		--ds2-ink: #18181b;
		--ds2-muted: #71717a;
		--ds2-border: #e4e4e7;
		--ds2-success: #0f9f6e;
		--ds2-warning: #d97706;
		--ds2-danger: #e11d48;
		--ds2-shadow: 0 30px 60px -15px rgba(24, 24, 27, 0.13);
		--ds2-glow: 0 24px 80px -28px rgba(35, 43, 255, 0.4);
		--ds2-font-system: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
		--ds2-display-size: 72px;
		--ds2-display-line: 72px;
		--ds2-display-weight: 500;
		--ds2-body-size: 16px;
		--ds2-body-line: 26px;
		--ds2-body-weight: 400;
		--ds2-label-size: 14px;
		--ds2-label-line: 20px;
		--ds2-label-weight: 500;
		background: var(--ds2-bg);
		color: var(--ds2-ink);
		font-family: var(--ds2-font-system);
		font-size: var(--ds2-body-size);
		font-weight: var(--ds2-body-weight);
		line-height: var(--ds2-body-line);
		letter-spacing: 0;
		overflow-x: clip;
	}

	:global(.dark) .ds2 {
		--ds2-bg: #09090b;
		--ds2-soft: #111116;
		--ds2-card: rgba(24, 24, 27, 0.74);
		--ds2-card-strong: #18181b;
		--ds2-ink: #ffffff;
		--ds2-muted: #a1a1aa;
		--ds2-border: #27272a;
		--ds2-shadow: 0 34px 80px -20px rgba(0, 0, 0, 0.55);
		background: var(--ds2-bg);
	}

	.ds2 :where(p, li, td, th, input, textarea, select) {
		font-family: var(--ds2-font-system);
		font-size: var(--ds2-body-size);
		font-weight: var(--ds2-body-weight);
		line-height: var(--ds2-body-line);
		letter-spacing: 0;
	}

	.ds2 :where(h1, h2, h3, h4, blockquote) {
		font-family: var(--ds2-font-system);
		font-weight: var(--ds2-display-weight);
		letter-spacing: 0;
	}

	.ds2-hero h1 {
		font-size: var(--ds2-display-size);
		font-weight: var(--ds2-display-weight);
		line-height: var(--ds2-display-line);
		letter-spacing: 0;
	}

	.ds2-container {
		width: min(100%, 1400px);
		margin: 0 auto;
		padding-inline: 24px;
	}

	@media (min-width: 1024px) {
		.ds2-container {
			padding-inline: 32px;
		}
	}

	.ds2-hero {
		background: linear-gradient(180deg, rgba(255, 255, 255, 0.62), rgba(246, 247, 251, 0.92));
	}

	:global(.dark) .ds2-hero {
		background: linear-gradient(180deg, rgba(9, 9, 11, 0.72), rgba(17, 17, 22, 0.92));
	}

	.ds2-section {
		padding-block: 80px;
	}

	.ds2-section-soft {
		background: rgba(246, 247, 251, 0.78);
	}

	:global(.dark) .ds2-section-soft {
		background: rgba(17, 17, 22, 0.78);
	}

	.ds2-section-dark {
		background: #18181b;
		color: #ffffff;
	}

	.ds2-section-dark .ds2-section-head > p,
	.ds2-section-dark .ds2-kicker {
		color: rgba(255, 255, 255, 0.68);
	}

	.ds2-section-head {
		display: grid;
		gap: 24px;
		align-items: end;
	}

	@media (min-width: 900px) {
		.ds2-section-head {
			grid-template-columns: 1fr minmax(320px, 520px);
		}
	}

	.ds2-section-head h2,
	.ds2-final-cta h2 {
		font-size: var(--ds2-display-size);
		font-weight: var(--ds2-display-weight);
		line-height: var(--ds2-display-line);
		letter-spacing: 0;
	}

	.ds2-section-head > p,
	.ds2-final-cta p {
		color: var(--ds2-muted);
		font-size: var(--ds2-body-size);
		font-weight: var(--ds2-body-weight);
		line-height: var(--ds2-body-line);
	}

	.ds2-kicker {
		display: inline-flex;
		align-items: center;
		min-height: 28px;
		border-radius: 9999px;
		background: rgba(35, 43, 255, 0.09);
		padding: 4px 12px;
		color: var(--ds2-primary);
		font-family: var(--ds2-font-system);
		font-size: var(--ds2-label-size);
		font-weight: var(--ds2-label-weight);
		line-height: var(--ds2-label-line);
		letter-spacing: 0;
	}

	.ds2-card,
	.ds2-dashboard,
	.ds2-quote,
	.ds2-price-card,
	.ds2-testimonial {
		min-width: 0;
		border: 1px solid rgba(255, 255, 255, 0.78);
		border-radius: 24px;
		background: var(--ds2-card);
		box-shadow: var(--ds2-shadow);
		backdrop-filter: blur(40px);
	}

	:global(.dark) .ds2-card,
	:global(.dark) .ds2-dashboard,
	:global(.dark) .ds2-quote,
	:global(.dark) .ds2-price-card,
	:global(.dark) .ds2-testimonial {
		border-color: rgba(255, 255, 255, 0.1);
	}

	.ds2-card,
	.ds2-dashboard,
	.ds2-price-card,
	.ds2-testimonial {
		padding: 24px;
	}

	@media (min-width: 768px) {
		.ds2-card,
		.ds2-dashboard,
		.ds2-price-card,
		.ds2-testimonial {
			padding: 32px;
		}
	}

	.ds2-dashboard {
		position: relative;
		border-radius: 40px;
		box-shadow: var(--ds2-shadow), var(--ds2-glow);
	}

	.ds2-metric {
		border: 1px solid var(--ds2-border);
		border-radius: 16px;
		background: rgba(255, 255, 255, 0.58);
		padding: 16px;
	}

	:global(.dark) .ds2-metric {
		background: rgba(255, 255, 255, 0.05);
	}

	.ds2-metric p,
	.ds2-stat-card p,
	.ds2-kpi-row span {
		font-size: 28px;
		line-height: 1;
		font-weight: 700;
	}

	.ds2-metric span,
	.ds2-stat-card span,
	.ds2-kpi-row p {
		margin-top: 8px;
		display: block;
		color: var(--ds2-muted);
		font-size: 13px;
		line-height: 1.45;
	}

	.ds2-progress {
		height: 8px;
		overflow: hidden;
		border-radius: 9999px;
		background: rgba(113, 113, 122, 0.16);
	}

	.ds2-progress span {
		display: block;
		height: 100%;
		border-radius: inherit;
		background: linear-gradient(90deg, var(--ds2-primary), var(--ds2-secondary));
	}

	.ds2-nav-pill,
	.ds2-chip,
	.ds2-status,
	.ds2-tab,
	.ds2-page-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 9999px;
		border: 1px solid var(--ds2-border);
		background: rgba(255, 255, 255, 0.7);
		color: var(--ds2-muted);
		font-size: 13px;
		font-weight: 700;
	}

	.ds2-chip {
		min-height: 34px;
		padding: 6px 14px;
		white-space: nowrap;
	}

	:global(.dark) .ds2-nav-pill,
	:global(.dark) .ds2-chip,
	:global(.dark) .ds2-tab,
	:global(.dark) .ds2-page-button {
		background: rgba(255, 255, 255, 0.05);
	}

	.ds2-nav-pill {
		min-height: 36px;
		padding: 0 14px;
		transition: border-color 150ms ease, color 150ms ease, background 150ms ease;
	}

	.ds2-nav-pill:hover,
	.ds2-page-button:hover,
	.ds2-tab.is-active,
	.ds2-page-button.is-active {
		border-color: transparent;
		background: rgba(35, 43, 255, 0.09);
		color: var(--ds2-primary);
	}

	.ds2-btn {
		--ds2-btn-fill: var(--ds2-primary);
		--ds2-btn-hover-color: #ffffff;
		--ds2-btn-hover-border: transparent;
		display: inline-flex;
		min-height: 44px;
		position: relative;
		align-items: center;
		justify-content: center;
		gap: 8px;
		overflow: hidden;
		border-radius: 9999px;
		padding: 0 30px;
		border: 1.5px solid transparent;
		font-size: 14px;
		font-weight: 700;
		line-height: 1;
		cursor: pointer;
		isolation: isolate;
		transition:
			border-color 800ms cubic-bezier(0.19, 1, 0.22, 1),
			background 800ms cubic-bezier(0.19, 1, 0.22, 1),
			color 800ms cubic-bezier(0.19, 1, 0.22, 1),
			transform 800ms cubic-bezier(0.19, 1, 0.22, 1);
	}

	.ds2-btn::before {
		position: absolute;
		top: 50%;
		left: 50%;
		z-index: 0;
		width: 16px;
		height: 16px;
		border-radius: 9999px;
		background: var(--ds2-btn-fill);
		opacity: 0;
		content: "";
		transform: translate(-50%, -50%);
		transition:
			width 800ms cubic-bezier(0.19, 1, 0.22, 1),
			height 800ms cubic-bezier(0.19, 1, 0.22, 1),
			opacity 800ms cubic-bezier(0.19, 1, 0.22, 1);
	}

	.ds2-btn::after {
		position: absolute;
		top: 50%;
		left: -28px;
		z-index: 2;
		color: currentColor;
		content: "→";
		font-size: 18px;
		line-height: 1;
		transform: translateY(-50%);
		transition:
			left 800ms cubic-bezier(0.34, 1.56, 0.64, 1),
			color 800ms cubic-bezier(0.34, 1.56, 0.64, 1);
	}

	.ds2-btn > span,
	:global(.ds2-btn > svg) {
		position: relative;
		z-index: 1;
	}

	.ds2-btn > span {
		transform: translateX(-4px);
		transition: transform 800ms ease;
	}

	:global(.ds2-btn > svg) {
		flex: 0 0 auto;
		transition:
			opacity 800ms cubic-bezier(0.34, 1.56, 0.64, 1),
			transform 800ms cubic-bezier(0.34, 1.56, 0.64, 1);
	}

	.ds2-btn:hover {
		border-color: var(--ds2-btn-hover-border);
		color: var(--ds2-btn-hover-color);
	}

	.ds2-btn:hover::before {
		width: 340px;
		height: 340px;
		opacity: 1;
	}

	.ds2-btn:hover::after {
		left: 16px;
		color: var(--ds2-btn-hover-color);
	}

	.ds2-btn:hover > span {
		transform: translateX(10px);
	}

	:global(.ds2-btn:hover > svg:last-child) {
		opacity: 0;
		transform: translateX(64px);
	}

	.ds2-btn:active {
		transform: scale(0.96);
	}

	.ds2-btn-primary {
		--ds2-btn-fill: var(--ds2-card-strong);
		--ds2-btn-hover-color: var(--ds2-primary);
		--ds2-btn-hover-border: var(--ds2-border);
		background: var(--ds2-primary);
		color: #ffffff;
		box-shadow: 0 18px 44px -18px rgba(35, 43, 255, 0.75);
	}

	.ds2-btn-primary:hover {
		background: var(--ds2-card-strong);
		box-shadow: none;
	}

	.ds2-btn-secondary {
		--ds2-btn-fill: var(--ds2-primary);
		--ds2-btn-hover-color: #ffffff;
		--ds2-btn-hover-border: transparent;
		border-color: var(--ds2-border);
		background: rgba(255, 255, 255, 0.76);
		color: var(--ds2-primary);
	}

	:global(.dark) .ds2-btn-secondary {
		background: rgba(255, 255, 255, 0.06);
	}

	.ds2-btn-secondary:hover,
	.ds2-btn-dark:hover {
		background: var(--ds2-primary);
	}

	.ds2-btn-dark {
		--ds2-btn-fill: var(--ds2-primary);
		--ds2-btn-hover-color: #ffffff;
		--ds2-btn-hover-border: transparent;
		background: var(--ds2-ink);
		color: var(--ds2-bg);
	}

	.ds2-btn-ghost {
		--ds2-btn-fill: var(--ds2-card-strong);
		--ds2-btn-hover-color: var(--ds2-ink);
		--ds2-btn-hover-border: var(--ds2-border);
		color: var(--ds2-muted);
	}

	.ds2-btn-ghost:hover {
		background: var(--ds2-card-strong);
	}

	.ds2-free-action {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 12px;
		margin-top: 24px;
	}

	.ds2-free-label {
		margin-left: 12px;
		color: var(--ds2-primary);
		font-size: clamp(42px, 6vw, 76px);
		font-weight: 800;
		line-height: 1;
		will-change: transform;
	}

	.ds2-free-action .ds2-btn-primary:hover + .ds2-free-label,
	.ds2-free-action .ds2-btn-primary:focus-visible + .ds2-free-label {
		animation: ds2-free-hop 720ms cubic-bezier(0.2, 0.8, 0.2, 1);
	}

	.ds2-icon-button {
		display: inline-grid;
		width: 44px;
		height: 44px;
		position: relative;
		place-items: center;
		overflow: hidden;
		border-radius: 9999px;
		border: 1px solid var(--ds2-border);
		background: rgba(255, 255, 255, 0.72);
		color: var(--ds2-muted);
		transition: border-color 150ms ease, color 150ms ease, background 150ms ease;
	}

	:global(.dark) .ds2-icon-button {
		background: rgba(255, 255, 255, 0.05);
	}

	.ds2-icon-main,
	.ds2-icon-clone {
		position: absolute;
		inset: 0;
		display: grid;
		place-items: center;
		transition:
			opacity 520ms cubic-bezier(0.34, 1.56, 0.64, 1),
			transform 520ms cubic-bezier(0.34, 1.56, 0.64, 1);
	}

	.ds2-icon-main {
		opacity: 1;
		transform: translateX(0);
	}

	.ds2-icon-clone {
		opacity: 0;
		transform: translateX(-120%);
	}

	.ds2-icon-button:hover,
	.ds2-icon-button.is-active {
		border-color: transparent;
		background: rgba(35, 43, 255, 0.09);
		color: var(--ds2-primary);
	}

	.ds2-icon-button:hover .ds2-icon-main {
		opacity: 0;
		transform: translateX(120%);
	}

	.ds2-icon-button:hover .ds2-icon-clone {
		opacity: 1;
		transform: translateX(0);
	}

	.ds2-status {
		padding: 5px 10px;
	}

	.ds2-status-success {
		border-color: transparent;
		background: rgba(15, 159, 110, 0.1);
		color: var(--ds2-success);
	}

	.ds2-status-info {
		border-color: transparent;
		background: rgba(35, 43, 255, 0.09);
		color: var(--ds2-primary);
	}

	.ds2-status-warning {
		border-color: transparent;
		background: rgba(217, 119, 6, 0.1);
		color: var(--ds2-warning);
	}

	.ds2-token-card {
		min-height: 248px;
	}

	.ds2-swatch {
		height: 96px;
		border-radius: 16px;
	}

	.ds2-scale-row {
		display: flex;
		min-height: 40px;
		align-items: center;
		gap: 12px;
		border-radius: 16px;
		border: 1px solid var(--ds2-border);
		padding: 8px 12px;
	}

	.ds2-scale-row span {
		display: block;
		height: 12px;
		border-radius: 9999px;
		background: var(--ds2-primary);
	}

	.ds2-scale-row b {
		font-size: 13px;
	}

	.ds2-radius-sample {
		display: grid;
		aspect-ratio: 1;
		place-items: center;
		border: 1px solid transparent;
		background: rgba(35, 43, 255, 0.08);
		color: var(--ds2-primary);
		font-size: 13px;
		font-weight: 700;
	}

	.ds2-type-row {
		display: grid;
		gap: 24px;
		border: 1px solid var(--ds2-border);
		border-radius: 24px;
		background: var(--ds2-card);
		padding: 24px;
		backdrop-filter: blur(40px);
	}

	@media (min-width: 900px) {
		.ds2-type-row {
			grid-template-columns: 220px 1fr;
			align-items: center;
		}
	}

	.ds2-type-name {
		color: var(--ds2-primary);
		font-size: 13px;
		font-weight: 700;
	}

	.ds2-type-example {
		max-width: 960px;
		line-height: 1.05;
		overflow-wrap: anywhere;
	}

	.ds2-quote {
		border-left: 4px solid var(--ds2-primary);
		padding: 32px;
	}

	.ds2-callout {
		border-radius: 24px;
		border: 1px solid var(--ds2-border);
		background: var(--ds2-card-strong);
		padding: 24px;
	}

	.ds2-callout p {
		font-size: 18px;
	}

	.ds2-callout span {
		margin-top: 10px;
		display: block;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.7;
	}

	.ds2-callout-info {
		border-color: var(--ds2-border);
	}

	.ds2-callout-success {
		border-color: var(--ds2-border);
	}

	.ds2-callout-warning {
		border-color: var(--ds2-border);
	}

	.ds2-tab {
		min-height: 40px;
		padding: 0 16px;
	}

	.ds2-page-button {
		width: 40px;
		height: 40px;
		position: relative;
		overflow: hidden;
		border-radius: 9999px;
	}

	.ds2-page-main,
	.ds2-page-clone {
		position: absolute;
		inset: 0;
		display: grid;
		place-items: center;
		transition:
			opacity 520ms cubic-bezier(0.34, 1.56, 0.64, 1),
			transform 520ms cubic-bezier(0.34, 1.56, 0.64, 1);
	}

	.ds2-page-main {
		opacity: 1;
		transform: translateX(0);
	}

	.ds2-page-clone {
		opacity: 0;
		transform: translateX(-120%);
	}

	.ds2-page-button:hover .ds2-page-main {
		opacity: 0;
		transform: translateX(120%);
	}

	.ds2-page-button:hover .ds2-page-clone {
		opacity: 1;
		transform: translateX(0);
	}

	.ds2-dark-pill {
		display: flex;
		min-height: 48px;
		align-items: center;
		gap: 10px;
		border-radius: 16px;
		border: 1px solid rgba(255, 255, 255, 0.12);
		background: rgba(255, 255, 255, 0.06);
		padding: 0 14px;
		color: rgba(255, 255, 255, 0.82);
		font-size: 14px;
	}

	.ds2-form {
		display: grid;
		gap: 16px;
		border-radius: 24px;
		background: #ffffff;
		padding: 24px;
		color: #18181b;
		box-shadow: 0 40px 80px -20px rgba(0, 0, 0, 0.5);
	}

	.ds2-form label:not(.ds2-check):not(.ds2-toggle) > span,
	.ds2-form-label {
		margin-bottom: 8px;
		display: block;
		font-size: var(--ds2-label-size);
		font-weight: var(--ds2-label-weight);
		line-height: var(--ds2-label-line);
		color: #3f3f46;
	}

	.ds2-form-field {
		position: relative;
	}

	.ds2-input {
		width: 100%;
		min-height: 48px;
		border-radius: 16px;
		border: 1px solid #e4e4e7;
		background: #ffffff;
		padding: 12px 14px;
		color: #18181b;
		font-size: 14px;
		outline: none;
		transition: border-color 150ms ease, box-shadow 150ms ease;
	}

	.ds2-input:focus {
		border-color: var(--ds2-border);
		box-shadow: 0 0 0 4px rgba(113, 113, 122, 0.16);
	}

	.ds2-check,
	.ds2-toggle {
		display: flex;
		min-height: 48px;
		align-items: center;
		gap: 10px;
		border-radius: 16px;
		border: 1px solid #e4e4e7;
		padding: 0 14px;
		color: #3f3f46;
		font-size: 14px;
	}

	.ds2-check input {
		width: 16px;
		height: 16px;
		accent-color: var(--ds2-primary);
	}

	.ds2-toggle input {
		position: absolute;
		opacity: 0;
		pointer-events: none;
	}

	.ds2-toggle span {
		position: relative;
		display: block;
		width: 42px;
		height: 24px;
		border-radius: 9999px;
		background: #d4d4d8;
	}

	.ds2-toggle span::after {
		position: absolute;
		top: 3px;
		left: 3px;
		width: 18px;
		height: 18px;
		border-radius: 9999px;
		background: #ffffff;
		content: "";
		transition: transform 150ms ease;
	}

	.ds2-toggle input:checked + span {
		background: var(--ds2-primary);
	}

	.ds2-toggle input:checked + span::after {
		transform: translateX(18px);
	}

	.ds2-select-note,
	.ds2-select-lab {
		border-radius: 24px;
		background: rgba(255, 255, 255, 0.06);
		padding: 24px;
		color: #ffffff;
	}

	.ds2-select-note {
		border: 1px solid rgba(255, 255, 255, 0.12);
	}

	.ds2-select-lab {
		border: 1px solid rgba(255, 255, 255, 0.12);
	}

	.ds2-select-note .ds2-kicker {
		background: rgba(255, 255, 255, 0.12);
		color: #ffffff;
	}

	.ds2-select-note h3 {
		margin-top: 14px;
		font-size: 32px;
		font-weight: 500;
		line-height: 1.08;
		letter-spacing: 0;
	}

	.ds2-select-note p:not(.ds2-kicker) {
		margin-top: 16px;
		color: rgba(255, 255, 255, 0.72);
		font-size: var(--ds2-body-size);
		font-weight: var(--ds2-body-weight);
		line-height: var(--ds2-body-line);
		letter-spacing: 0;
	}

	.ds2-select {
		position: relative;
	}

	.ds2-select-trigger {
		display: flex;
		width: 100%;
		min-height: 38px;
		align-items: center;
		justify-content: space-between;
		gap: 10px;
		border-radius: 8px;
		border: 1px solid var(--ds2-border);
		background: #ffffff;
		padding: 0 14px;
		color: #18181b;
		text-align: left;
		box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
		outline: none;
		transition:
			border-color 180ms ease,
			box-shadow 180ms ease,
			background 180ms ease,
			color 180ms ease;
	}

	.ds2-select-trigger-sm {
		min-height: 32px;
		padding: 0 10px;
		font-size: 12px;
	}

	.ds2-select-trigger-lg {
		min-height: 44px;
		padding: 0 16px;
		font-size: 14px;
	}

	.ds2-select-trigger:hover,
	.ds2-select-trigger:focus-visible,
	.ds2-select[data-open="true"] .ds2-select-trigger {
		border-color: var(--ds2-border);
		box-shadow: 0 0 0 3px rgba(113, 113, 122, 0.18);
	}

	.ds2-select-form .ds2-select-trigger {
		min-height: 48px;
		border-radius: 16px;
		padding: 12px 14px;
	}

	.ds2-select-form .ds2-select-content {
		top: calc(100% + 8px);
	}

	:global(.ds2-select-chevron) {
		flex: 0 0 auto;
		opacity: 0.6;
		transform: rotate(90deg);
		transition:
			opacity 200ms ease,
			transform 200ms ease;
	}

	:global(.ds2-select[data-open="true"] .ds2-select-chevron) {
		opacity: 1;
		transform: rotate(-90deg);
	}

	.ds2-select-content {
		position: absolute;
		top: calc(100% + 6px);
		right: 0;
		left: 0;
		z-index: 50;
		overflow: hidden;
		border-radius: 10px;
		border: 1px solid var(--ds2-border);
		background: #ffffff;
		color: #18181b;
		box-shadow: 0 18px 48px -22px rgba(0, 0, 0, 0.32);
		opacity: 0;
		pointer-events: none;
		transform: translateY(-8px) scale(0.96);
		transform-origin: top center;
		visibility: hidden;
		transition:
			opacity 180ms ease,
			transform 180ms ease,
			visibility 180ms ease;
	}

	.ds2-select[data-open="true"] .ds2-select-content {
		animation: ds2-select-in 180ms ease-out;
		opacity: 1;
		pointer-events: auto;
		transform: translateY(0) scale(1);
		visibility: visible;
	}

	.ds2-select-viewport {
		max-height: 260px;
		overflow: auto;
		padding: 6px;
	}

	.ds2-select-scroll {
		display: grid;
		width: 100%;
		min-height: 24px;
		place-items: center;
		color: var(--ds2-muted);
		cursor: default;
	}

	:global(.ds2-select-scroll-up svg) {
		transform: rotate(-90deg);
	}

	:global(.ds2-select-scroll-down svg) {
		transform: rotate(90deg);
	}

	.ds2-select-label {
		padding: 6px 8px 6px 32px;
		color: var(--ds2-muted);
		font-size: 12px;
		font-weight: 500;
		line-height: 18px;
	}

	.ds2-select-item {
		position: relative;
		display: flex;
		width: 100%;
		min-height: 36px;
		align-items: center;
		border-radius: 6px;
		padding: 7px 10px 7px 32px;
		color: #18181b;
		text-align: left;
		outline: none;
		transition:
			background 150ms ease,
			color 150ms ease;
	}

	.ds2-select-item:hover,
	.ds2-select-item:focus-visible {
		background: rgba(35, 43, 255, 0.09);
	}

	.ds2-select-item.is-selected {
		color: var(--ds2-primary);
	}

	.ds2-select-indicator {
		position: absolute;
		top: 50%;
		left: 10px;
		display: grid;
		width: 14px;
		height: 14px;
		place-items: center;
		color: var(--ds2-primary);
		opacity: 0;
		transform: translateY(-50%);
		transition: opacity 150ms ease;
	}

	.ds2-select-item.is-selected .ds2-select-indicator {
		opacity: 1;
	}

	.ds2-select-separator {
		height: 1px;
		margin: 6px;
		background: var(--ds2-border);
	}

	@keyframes ds2-select-in {
		from {
			opacity: 0;
			transform: translateY(-8px) scale(0.96);
		}
		to {
			opacity: 1;
			transform: translateY(0) scale(1);
		}
	}

	.ds2-mini-hero {
		display: grid;
		gap: 32px;
		align-items: center;
		border-radius: 40px;
		background: linear-gradient(135deg, var(--ds2-card-strong), rgba(35, 43, 255, 0.08));
		padding: 32px;
		box-shadow: var(--ds2-shadow);
	}

	@media (min-width: 900px) {
		.ds2-mini-hero {
			grid-template-columns: 1fr 420px;
			padding: 48px;
		}
	}

	.ds2-mini-hero h3 {
		margin-top: 12px;
		max-width: 720px;
		font-size: 48px;
		line-height: 1.02;
	}

	.ds2-mini-hero p {
		margin-top: 20px;
		max-width: 660px;
		color: var(--ds2-muted);
		font-size: 18px;
		line-height: 1.75;
	}

	.ds2-mini-visual {
		border-radius: 24px;
		border: 1px solid rgba(255, 255, 255, 0.78);
		background: rgba(255, 255, 255, 0.72);
		padding: 20px;
		backdrop-filter: blur(40px);
	}

	:global(.dark) .ds2-mini-visual {
		border-color: rgba(255, 255, 255, 0.1);
		background: rgba(255, 255, 255, 0.06);
	}

	.ds2-mini-line {
		height: 16px;
		width: 78%;
		border-radius: 9999px;
		background: rgba(35, 43, 255, 0.18);
	}

	.ds2-mini-line.is-short {
		margin-top: 12px;
		width: 52%;
	}

	.ds2-mini-tile {
		margin-top: 20px;
		display: grid;
		min-height: 96px;
		place-items: center;
		border-radius: 20px;
		background: var(--ds2-ink);
		color: var(--ds2-bg);
		font-size: 22px;
		font-weight: 700;
	}

	.ds2-logo-strip {
		display: grid;
		gap: 1px;
		overflow: hidden;
		border-radius: 24px;
		border: 1px solid var(--ds2-border);
		background: var(--ds2-border);
	}

	@media (min-width: 768px) {
		.ds2-logo-strip {
			grid-template-columns: repeat(5, 1fr);
		}
	}

	.ds2-logo-strip span {
		display: grid;
		min-height: 84px;
		place-items: center;
		background: var(--ds2-card-strong);
		padding: 16px;
		text-align: center;
		color: var(--ds2-muted);
		font-size: 15px;
		font-weight: 700;
	}

	.ds2-service-card,
	.ds2-stat-card,
	.ds2-case-row,
	.ds2-step,
	.ds2-kpi-row,
	.ds2-faq {
		border: 1px solid var(--ds2-border);
		background: var(--ds2-card-strong);
	}

	.ds2-service-card {
		min-width: 0;
		min-height: 260px;
		border-radius: 24px;
		padding: 28px;
		transition: transform 150ms ease, border-color 150ms ease, box-shadow 150ms ease;
	}

	.ds2-service-card:hover {
		transform: translateY(-2px);
		border-color: var(--ds2-border);
		box-shadow: var(--ds2-glow);
	}

	.ds2-service-icon {
		display: grid;
		width: 48px;
		height: 48px;
		place-items: center;
		color: var(--ds2-primary);
	}

	.ds2-service-icon :global(svg) {
		width: 48px;
		height: 48px;
	}

	.ds2-stat-card {
		border-radius: 24px;
		padding: 24px;
	}

	.ds2-price-card {
		display: flex;
		min-height: 430px;
		flex-direction: column;
	}

	.ds2-price-card.is-featured {
		border-color: transparent;
		background: linear-gradient(180deg, rgba(35, 43, 255, 0.1), var(--ds2-card));
		box-shadow: var(--ds2-shadow), var(--ds2-glow);
	}

	.ds2-price-card ul {
		margin-bottom: 28px;
	}

	.ds2-price-card .ds2-btn {
		margin-top: auto;
	}

	.bitrix-wide-head {
		align-items: start;
	}

	@media (min-width: 900px) {
		.bitrix-wide-head {
			grid-template-columns: 1fr;
		}
	}

	.bitrix-wide-head h2 {
		max-width: none;
	}

	.bitrix-wide-head > p {
		max-width: 980px;
	}

	.bitrix-problems,
	.bitrix-prices,
	.bitrix-services {
		display: grid;
		gap: 20px;
		margin-top: 44px;
	}

	@media (min-width: 768px) {
		.bitrix-problems {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.bitrix-services {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (min-width: 1100px) {
		.bitrix-problems {
			grid-template-columns: repeat(3, minmax(0, 1fr));
		}

		.bitrix-prices {
			grid-template-columns: repeat(4, minmax(0, 1fr));
			gap: 16px;
			align-items: stretch;
		}

		.bitrix-prices .bitrix-audit-card {
			grid-column: 1 / -1;
			margin-bottom: 32px;
		}

		.bitrix-prices .ds2-price-card:not(.bitrix-audit-card) {
			height: 100%;
		}

		.bitrix-prices .ds2-price-card.is-featured {
			position: relative;
			z-index: 1;
			margin-top: -18px;
			transform: scale(1.018);
			transform-origin: center bottom;
		}

		.bitrix-services {
			grid-template-columns: repeat(4, minmax(0, 1fr));
		}
	}

	.bitrix-card-top {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 20px;
	}

	.bitrix-problem-card {
		display: flex;
		min-height: 300px;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		text-align: center;
	}

	.bitrix-problem-card p {
		margin-top: 22px;
		color: var(--ds2-primary);
		font-size: 14px;
		font-weight: 700;
	}

	.bitrix-problem-card h3 {
		margin-top: 8px;
		font-size: 24px;
		line-height: 1.14;
	}

	.bitrix-audit-card {
		min-height: 0;
		margin-top: 0;
	}

	.bitrix-audit-card .ds2-btn {
		margin-top: 24px;
	}

	.bitrix-audit-body {
		display: grid;
		gap: 18px;
		margin-top: 24px;
	}

	@media (min-width: 768px) {
		.bitrix-audit-card {
			display: grid;
			grid-template-columns: minmax(0, 1fr) auto;
			gap: 20px 32px;
			align-items: start;
		}

		.bitrix-audit-card .bitrix-card-top {
			grid-column: 1 / -1;
		}

		.bitrix-audit-body {
			margin-top: 0;
		}

		.bitrix-audit-card ul {
			margin-top: 0;
		}

		.bitrix-audit-card .ds2-btn {
			grid-column: 1 / -1;
			width: max-content;
		}
	}

	@media (min-width: 1100px) {
		.bitrix-audit-card {
			grid-template-columns: 180px 1fr minmax(320px, 0.82fr) auto;
			align-items: center;
		}

		.bitrix-audit-card .bitrix-card-top {
			grid-column: auto;
		}

		.bitrix-audit-card .ds2-btn {
			grid-column: auto;
			justify-self: end;
			margin-top: 0;
		}
	}

	.ds2-price-card h3 {
		margin-top: 24px;
		font-size: 26px;
		line-height: 1.12;
	}

	.bitrix-audit-card h3 {
		margin-top: 0;
	}

	.bitrix-price {
		margin-top: 12px;
		color: var(--ds2-primary);
		font-size: 30px;
		font-weight: 700;
		line-height: 1.1;
	}

	.bitrix-card-text {
		margin-top: 12px;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.65;
	}

	.ds2-price-card ul,
	.bitrix-services ul {
		display: grid;
		gap: 12px;
		margin-top: 24px;
	}

	.ds2-price-card li,
	.bitrix-services li {
		display: flex;
		gap: 10px;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.5;
	}

	.ds2-price-card li :global(svg),
	.bitrix-services li :global(svg),
	.bitrix-reasons :global(svg) {
		flex: 0 0 auto;
		color: var(--ds2-success);
	}

	.bitrix-pattern-copy h3 {
		margin-top: 14px;
		font-size: 52px;
		font-weight: var(--ds2-display-weight);
		line-height: 54px;
	}

	.bitrix-pattern-copy p:not(.ds2-kicker) {
		margin-top: 22px;
		color: var(--ds2-muted);
		font-size: var(--ds2-body-size);
		line-height: var(--ds2-body-line);
	}

	.bitrix-pattern-copy .ds2-btn {
		margin-top: 28px;
	}

	.bitrix-timeline {
		overflow: hidden;
		border-radius: 24px;
		border: 1px solid rgba(255, 255, 255, 0.78);
		background: var(--ds2-card);
		box-shadow: var(--ds2-shadow);
		backdrop-filter: blur(40px);
	}

	:global(.dark) .bitrix-timeline {
		border-color: rgba(255, 255, 255, 0.1);
	}

	.bitrix-timeline-item {
		display: grid;
		grid-template-columns: 52px 1fr;
		min-height: 132px;
		transition: background 180ms ease;
	}

	.bitrix-timeline-item:hover {
		background: rgba(35, 43, 255, 0.05);
	}

	.bitrix-timeline-date {
		grid-column: 2;
		grid-row: 1;
		padding: 22px 22px 0 0;
		color: var(--ds2-muted);
	}

	.bitrix-timeline-date span {
		display: block;
		font-size: 12px;
		font-weight: 700;
		line-height: 16px;
		text-transform: uppercase;
	}

	.bitrix-timeline-date time {
		display: block;
		margin-top: 2px;
		color: var(--ds2-primary);
		font-size: 18px;
		font-weight: 700;
		line-height: 24px;
	}

	.bitrix-timeline-track {
		position: relative;
		grid-column: 1;
		grid-row: 1 / span 2;
		min-height: 132px;
	}

	.bitrix-timeline-line {
		position: absolute;
		top: 0;
		bottom: 0;
		left: 50%;
		border-left: 1px solid var(--ds2-border);
		transform: translateX(-50%);
	}

	.bitrix-timeline-item:first-child .bitrix-timeline-line {
		top: 34px;
	}

	.bitrix-timeline-item:last-child .bitrix-timeline-line {
		bottom: calc(100% - 34px);
	}

	.bitrix-timeline-dot {
		position: absolute;
		top: 22px;
		left: 50%;
		z-index: 1;
		display: grid;
		width: 30px;
		height: 30px;
		place-items: center;
		border-radius: 9999px;
		border: 1px solid rgba(35, 43, 255, 0.22);
		background: var(--ds2-primary);
		color: #ffffff;
		box-shadow: 0 0 0 7px rgba(35, 43, 255, 0.1);
		transform: translateX(-50%);
		transition: box-shadow 180ms ease, transform 180ms ease;
	}

	.bitrix-timeline-item:hover .bitrix-timeline-dot {
		box-shadow: 0 0 0 10px rgba(35, 43, 255, 0.14);
		transform: translateX(-50%) scale(1.04);
	}

	.bitrix-timeline-content {
		grid-column: 2;
		grid-row: 2;
		padding: 6px 22px 24px 0;
	}

	.bitrix-timeline-content h3 {
		color: var(--ds2-ink);
		font-size: 24px;
		font-weight: 500;
		line-height: 1.18;
	}

	.bitrix-timeline-content p {
		margin-top: 8px;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.65;
	}

	@media (min-width: 768px) {
		.bitrix-timeline {
			padding-block: 8px;
		}

		.bitrix-timeline-item {
			grid-template-columns: 128px 60px 1fr;
			min-height: 116px;
			align-items: start;
		}

		.bitrix-timeline-date {
			grid-column: 1;
			grid-row: 1;
			padding: 28px 0 0 24px;
		}

		.bitrix-timeline-track {
			grid-column: 2;
			grid-row: 1;
			min-height: 116px;
		}

		.bitrix-timeline-content {
			grid-column: 3;
			grid-row: 1;
			padding: 24px 28px 24px 0;
		}
	}

	.bitrix-logo-marquee {
		position: relative;
		margin-top: 44px;
		overflow: hidden;
		mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
	}

	.bitrix-logo-track {
		display: flex;
		width: max-content;
		will-change: transform;
		animation: bitrix-logo-marquee 18s linear infinite;
	}

	.bitrix-logo-group {
		display: flex;
		flex: 0 0 auto;
		gap: 40px;
		padding-right: 40px;
	}

	.bitrix-logo-card {
		display: grid;
		width: clamp(240px, 24vw, 340px);
		min-height: 150px;
		place-items: center;
		border: 0;
		background: transparent;
		padding: 10px;
	}

	.bitrix-logo-card img {
		max-width: 100%;
		max-height: 108px;
		object-fit: contain;
	}

	@keyframes bitrix-logo-marquee {
		from {
			transform: translateX(0);
		}

		to {
			transform: translate3d(-33.333333%, 0, 0);
		}
	}

	@keyframes ds2-free-hop {
		0%,
		100% {
			transform: translateY(0) rotate(0deg) scale(1);
		}

		22% {
			transform: translateY(-18px) rotate(-3deg) scale(1.06);
		}

		42% {
			transform: translateY(0) rotate(2deg) scale(1);
		}

		58% {
			transform: translateY(-10px) rotate(-1deg) scale(1.03);
		}

		76% {
			transform: translateY(0) rotate(1deg) scale(1);
		}

		88% {
			transform: translateY(-4px) rotate(0deg) scale(1.01);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.bitrix-logo-track {
			animation: none;
		}

		.ds2-free-action .ds2-btn-primary:hover + .ds2-free-label,
		.ds2-free-action .ds2-btn-primary:focus-visible + .ds2-free-label {
			animation: none;
		}
	}

	.bitrix-services .ds2-service-card {
		min-height: 330px;
	}

	.bitrix-services h3 {
		margin-top: 22px;
		font-size: 26px;
		line-height: 1.12;
	}

	.bitrix-services .is-accent {
		background: #18181b;
		color: #ffffff;
	}

	.bitrix-services .is-accent .ds2-service-icon {
		color: #ffffff;
	}

	.bitrix-services .is-accent li {
		color: rgba(255, 255, 255, 0.72);
	}

	.bitrix-services .is-accent li :global(svg) {
		color: var(--ds2-secondary);
	}

	.bitrix-reasons-card {
		display: grid;
		gap: 28px;
		align-items: center;
	}

	.bitrix-reasons-card .bitrix-reasons {
		margin-top: 0;
	}

	@media (min-width: 900px) {
		.bitrix-reasons-card {
			grid-template-columns: minmax(0, 0.86fr) minmax(360px, 0.72fr);
			padding: 40px;
		}
	}

	.bitrix-reasons {
		display: grid;
		gap: 12px;
		margin-top: 28px;
	}

	.bitrix-reasons span {
		display: flex;
		align-items: center;
		gap: 10px;
		border-radius: 16px;
		border: 1px solid rgba(255, 255, 255, 0.12);
		background: rgba(255, 255, 255, 0.06);
		padding: 10px 12px;
		color: rgba(255, 255, 255, 0.76);
		font-size: 14px;
	}

	@media (max-width: 767px) {
		.bitrix-problems,
		.bitrix-prices,
		.bitrix-services {
			display: flex;
			grid-template-columns: none;
			gap: 16px;
			width: calc(100% + 48px);
			margin-right: -24px;
			margin-left: -24px;
			overflow-x: auto;
			overflow-y: visible;
			padding: 8px 24px 36px;
			margin-bottom: -24px;
			scroll-padding-inline: 24px;
			scroll-snap-type: x mandatory;
			-webkit-overflow-scrolling: touch;
			scrollbar-width: none;
		}

		.bitrix-problems::-webkit-scrollbar,
		.bitrix-prices::-webkit-scrollbar,
		.bitrix-services::-webkit-scrollbar {
			display: none;
		}

		.bitrix-problems > *,
		.bitrix-prices > *,
		.bitrix-services > * {
			flex: 0 0 min(78vw, 340px);
			scroll-snap-align: start;
		}

		.bitrix-problem-card,
		.bitrix-services .ds2-service-card {
			min-height: 320px;
		}

		.bitrix-prices {
			padding-top: 28px;
			padding-bottom: 88px;
			margin-bottom: -72px;
		}
	}

	@media (min-width: 640px) and (max-width: 767px) {
		.bitrix-problems > *,
		.bitrix-prices > *,
		.bitrix-services > * {
			flex-basis: min(44vw, 340px);
		}
	}

	.ds2-step {
		display: grid;
		grid-template-columns: auto auto 1fr;
		gap: 14px;
		align-items: start;
		border-radius: 18px;
		padding: 16px;
	}

	.ds2-step-number {
		color: var(--ds2-primary);
		font-size: 13px;
		font-weight: 700;
	}

	.ds2-step p {
		font-weight: 700;
	}

	.ds2-step span {
		margin-top: 4px;
		display: block;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.55;
	}

	.ds2-case-row {
		display: grid;
		grid-template-columns: 1fr auto;
		gap: 20px;
		align-items: center;
		border-radius: 20px;
		padding: 20px;
		transition: border-color 150ms ease, transform 150ms ease;
	}

	.ds2-case-row:hover {
		transform: translateX(2px);
		border-color: var(--ds2-border);
	}

	.ds2-table {
		width: 100%;
		min-width: 620px;
		border-collapse: collapse;
		font-size: 14px;
	}

	.ds2-table th,
	.ds2-table td {
		border-bottom: 1px solid var(--ds2-border);
		padding: 14px;
		text-align: left;
	}

	.ds2-table th {
		color: var(--ds2-primary);
		font-weight: 700;
	}

	.ds2-testimonial {
		background: #18181b;
		color: #ffffff;
	}

	.ds2-testimonial blockquote {
		margin-top: 18px;
		font-size: 28px;
		line-height: 1.22;
	}

	.ds2-avatar {
		display: grid;
		width: 48px;
		height: 48px;
		place-items: center;
		border-radius: 9999px;
		background: var(--ds2-primary);
		color: #ffffff;
		font-weight: 700;
	}

	.ds2-testimonial span {
		color: rgba(255, 255, 255, 0.62);
		font-size: 14px;
	}

	.ds2-faq {
		overflow: hidden;
		border-radius: 20px;
		padding: 20px;
		transition:
			background 200ms ease,
			border-color 200ms ease,
			box-shadow 200ms ease;
	}

	.ds2-faq summary {
		display: flex;
		cursor: pointer;
		list-style: none;
		align-items: center;
		justify-content: space-between;
		gap: 20px;
		margin: -20px;
		padding: 20px;
		font-weight: 700;
		transition: color 200ms ease;
	}

	.ds2-faq[open] summary {
		margin-bottom: 0;
	}

	.ds2-faq summary::-webkit-details-marker {
		display: none;
	}

	.ds2-faq summary span {
		transition: color 200ms ease;
	}

	:global(.ds2-faq summary svg) {
		flex: 0 0 auto;
		opacity: 0.6;
		transition:
			opacity 200ms ease,
			transform 200ms ease;
	}

	.ds2-faq:hover {
		box-shadow: 0 18px 44px -28px rgba(24, 24, 27, 0.28);
	}

	.ds2-faq[open] {
		background: var(--ds2-card);
	}

	:global(.ds2-faq[open] summary svg) {
		opacity: 1;
		transform: rotate(180deg);
	}

	.ds2-faq-content {
		display: grid;
		grid-template-rows: 0fr;
		opacity: 0;
		transition:
			grid-template-rows 220ms ease,
			opacity 200ms ease;
	}

	.ds2-faq[open] .ds2-faq-content {
		grid-template-rows: 1fr;
		opacity: 1;
		animation: ds2-accordion-down 220ms ease-out;
	}

	.ds2-faq.is-closing .ds2-faq-content {
		animation: ds2-accordion-up 220ms ease-in;
	}

	.ds2-faq-content p {
		min-height: 0;
		overflow: hidden;
		margin-top: 12px;
		color: var(--ds2-muted);
		font-size: 14px;
		line-height: 1.7;
	}

	@keyframes ds2-accordion-down {
		from {
			opacity: 0;
			transform: translateY(-4px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes ds2-accordion-up {
		from {
			opacity: 1;
			transform: translateY(0);
		}

		to {
			opacity: 0;
			transform: translateY(-4px);
		}
	}

	.ds2-kpi-row {
		display: flex;
		min-height: 80px;
		align-items: center;
		justify-content: space-between;
		gap: 16px;
		border-radius: 18px;
		padding: 16px;
	}

	.ds2-kpi-row b {
		color: var(--ds2-success);
	}

	.ds2-range {
		width: 100%;
		accent-color: var(--ds2-primary);
	}

	.ds2-timeline-row {
		display: grid;
		grid-template-columns: 54px 12px 1fr;
		gap: 12px;
		align-items: start;
	}

	.ds2-timeline-row time {
		color: var(--ds2-muted);
		font-size: 13px;
	}

	.ds2-timeline-row span {
		margin-top: 4px;
		width: 12px;
		height: 12px;
		border-radius: 9999px;
		background: var(--ds2-primary);
		box-shadow: 0 0 0 5px rgba(35, 43, 255, 0.12);
	}

	.ds2-timeline-row p {
		line-height: 1.55;
	}

	.ds2-final-cta {
		display: grid;
		gap: 32px;
		align-items: center;
		border-radius: 40px;
		background: linear-gradient(135deg, #18181b, #232bff);
		padding: 32px;
		color: #ffffff;
		box-shadow: var(--ds2-glow);
	}

	@media (min-width: 900px) {
		.ds2-final-cta {
			grid-template-columns: 1fr auto;
			padding: 48px;
		}
	}

	.ds2-final-cta .ds2-kicker {
		background: rgba(255, 255, 255, 0.12);
		color: #ffffff;
	}

	.ds2-final-cta p {
		margin-top: 18px;
		max-width: 720px;
		color: rgba(255, 255, 255, 0.78);
	}

	.ds2-final-cta .ds2-btn-primary {
		background: #ffffff;
		color: #18181b;
	}

	.ds2 :where(.ds2-kicker, .ds2-btn, .ds2-nav-pill, .ds2-chip, .ds2-status, .ds2-tab, .ds2-page-button, .ds2-type-name, .ds2-scale-row b, .ds2-dark-pill, .ds2-check, .ds2-toggle, .ds2-select-trigger, .ds2-select-item, .ds2-select-label, .ds2-form-label, .ds2-form label:not(.ds2-check):not(.ds2-toggle) > span, .ds2-timeline-row time) {
		font-family: var(--ds2-font-system);
		font-size: var(--ds2-label-size);
		font-weight: var(--ds2-label-weight);
		line-height: var(--ds2-label-line);
		letter-spacing: 0;
	}

	.ds2 :where(.ds2-section-head > p, .ds2-final-cta p, .ds2-mini-hero p, .ds2-callout span, .ds2-step span, .ds2-faq p, .ds2-case-row p, .ds2-testimonial span, .ds2-metric span, .ds2-stat-card span, .ds2-kpi-row p) {
		font-family: var(--ds2-font-system);
		font-size: var(--ds2-body-size);
		font-weight: var(--ds2-body-weight);
		line-height: var(--ds2-body-line);
		letter-spacing: 0;
	}

	.ds2-mini-hero h3 {
		font-size: var(--ds2-display-size);
		font-weight: var(--ds2-display-weight);
		line-height: var(--ds2-display-line);
		letter-spacing: 0;
	}

	.ds2 :where(b, strong, .font-bold, .ds2-metric p, .ds2-stat-card p, .ds2-kpi-row span, .ds2-avatar, .ds2-faq summary, .ds2-step p, .ds2-table th) {
		font-weight: var(--ds2-display-weight);
	}

	@media (max-width: 640px) {
		.ds2-section {
			padding-block: 56px;
		}

		.ds2-hero h1,
		.ds2-section-head h2,
		.ds2-final-cta h2,
		.ds2-mini-hero h3,
		.bitrix-pattern-copy h3 {
			font-size: 44px;
			line-height: 44px;
		}

		.ds2-type-example {
			font-size: min(var(--type-size, 32px), 42px) !important;
			line-height: min(var(--type-line, 40px), 44px) !important;
		}
	}
</style>
